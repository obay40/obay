import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export const metadata = { title: "Favoriten" };

export default function FavoritesPage() {
  return (
    <PagePlaceholder
      title="Favoriten"
      description="Die Merkliste für Fahrzeuge entsteht in Phase 7, sobald Listings verfügbar sind."
      phase="Phase 7"
    />
  );
}
