import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export const metadata = { title: "Hilfe" };

export default function HelpPage() {
  return (
    <PagePlaceholder
      title="Hilfe"
      description="Ein Hilfebereich folgt."
      phase="Redaktionell offen"
    />
  );
}
