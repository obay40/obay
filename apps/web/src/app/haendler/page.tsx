import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export const metadata = { title: "Für Händler" };

export default function DealerLandingPage() {
  return (
    <PagePlaceholder
      title="Für Händler"
      description="Händlerregistrierung, -verifizierung und das Händlerdashboard entstehen in Phase 8."
      phase="Phase 8"
    />
  );
}
