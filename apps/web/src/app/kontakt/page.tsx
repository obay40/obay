import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export const metadata = { title: "Kontakt" };

export default function ContactPage() {
  return (
    <PagePlaceholder
      title="Kontakt"
      description="Ein Kontaktformular folgt, sobald das Nachrichtensystem (Phase 7) verfügbar ist."
      phase="Phase 7"
    />
  );
}
