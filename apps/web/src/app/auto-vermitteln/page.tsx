import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export const metadata = { title: "Auto vermitteln" };

export default function MediationPage() {
  return (
    <PagePlaceholder
      title="Auto vermitteln"
      description="Der Vermittlungs-Workflow entsteht in Phase 5, aufbauend auf der Fahrzeugerfassung aus Phase 3."
      phase="Phase 5"
    />
  );
}
